name: Hello Github Action
env:
  REPO_OWNER: NgocHai220998
  REPO_NAME: example-event-dispatcher-workflows
  GITHUB_TOKEN: ${{ secrets.TOKEN }}
on:
  push:
  repository_dispatch:
    types: [run-hello]
jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Hello World
        run: echo "Hello GithubAction"
