name: Hello Github Action
on:
  push:
  workflow_dispatch:
    inputs:
  repository_dispatch:
    types: [run-hello]
jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Hello World
        run: echo "Hello GithubAction"
